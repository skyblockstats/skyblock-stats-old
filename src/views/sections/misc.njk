<ul>
{%- set count = 0 -%}
{%- for stat in data.member.stats | sort(true, true, 'value') -%}
	{%- if stat.category == 'misc' -%}
		<li>{{ stat.categorizedName|clean }}: {{ stat.value }}</li>
		{%- set count = count + 1 -%}
	{%- endif -%}
{%- endfor -%}
</ul>